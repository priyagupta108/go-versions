name: Get Go versions
on:
  schedule:
    - cron: '0 3,15 * * *'
  workflow_dispatch:
  push:
    branches: [ test-ubuntu24 ]
jobs:
  get-new-go-versions:
    uses: priyagupta108/versions-package-tools/.github/workflows/get-new-tool-versions.yml@ubuntu-24-test
    with:
      tool-name: "Go"
      image-url: "https://go.dev/images/gopher-footer.jpg"
    secrets: inherit